from setuptools import find_packages, setup

setup(
    name="src",
    version="0.0.1",
    author="phani",
    author_email="phani@example.com",
    packages=find_packages(),
    install_requires=[],
)